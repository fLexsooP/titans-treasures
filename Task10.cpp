#include <iostream>

int main() {
    std::cout << "Update the buyer UI view to display the searched products from recently uploaded to old & vice versa when filtering searches by upload date" << std::endl;
    return 0;
}